# Vaultwarden - Environment Variables
# Generate admin token with: openssl rand -base64 48
VAULTWARDEN_ADMIN_TOKEN=change_this_admin_token

# Timezone
TZ=Europe/Warsaw

# Domain (will be used with nginx proxy)
DOMAIN=home.local

# Additional settings
SIGNUPS_ALLOWED=false
WEBSOCKET_ENABLED=true
