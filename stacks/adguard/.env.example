# Skopiuj jako .env obok docker-compose.yml. Plik .env jest w .gitignore.
#
# Korzen danych wszystkich stackow na hoscie. Przy migracji na x86
# zmieniasz te jedna wartosc, a nie sciezki w kazdym pliku compose.
DATA_ROOT=/srv/homelab/data
